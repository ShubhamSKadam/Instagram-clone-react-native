import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProfilePostReels = () => {
  return (
    <View>
      <Text>Start creating reels</Text>
    </View>
  );
};

export default ProfilePostReels;

const styles = StyleSheet.create({});
